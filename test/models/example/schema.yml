models:
- name: dt1_v
  config:
    elementary:
      timestamp_column: 'ts'
  tests:
  - elementary.table_anomalies:
      table_anomalies:
      - row_count
      tags:
      - table_anomalies
version: 2
